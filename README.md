# fire-emblem-rl
Trying to train a Reinforcement Learning agent to play Fire Emblem 8.

fe8_state.lua: mGBA script that reads the live game state and serves it via HTTP.
fe8_bridge.py: Python client that reads the live game state from mGBA via HTTP.
